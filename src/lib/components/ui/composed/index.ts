/**
 * Composed UI - Composants composés à partir de primitives
 *
 * Ces composants assemblent plusieurs primitives pour former des composants
 * plus complexes et réutilisables (molecules dans Atomic Design).
 *
 * @module composed
 */

export { default as Card } from './Card.svelte';
